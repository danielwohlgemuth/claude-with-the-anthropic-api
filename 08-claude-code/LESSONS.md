# Claude Code

Create an initial CLAUDE.md file with `/init`. This causes Claude to analyze the project and write general information to be included in future sessions.

Add custom instructions to memory by starting them with `#`. These used to be added to the CLAUDE.md file.