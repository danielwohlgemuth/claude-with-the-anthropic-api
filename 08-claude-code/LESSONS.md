# Claude Code

Create an initial CLAUDE.md file with `/init`. This will make Claude analyze the project and general information about the project to be included in future sessions.

Add specific instruction to memory by starting the instructions with `#`. It used to add the instruction to the CLAUDE.md file.