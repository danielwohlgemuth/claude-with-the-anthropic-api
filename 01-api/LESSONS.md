# API Lessons
- The assistant message can have leading text to guide the model to a specific response
- A stop sequence can be defined to prevent the model from generating additional text, which is especially useful for response data like JSON